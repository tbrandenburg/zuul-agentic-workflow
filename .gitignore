# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
build/
coverage/
*.tsbuildinfo

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Docker / Zuul runtime
zuul/.zuul-scheduler/
zuul/logs/
*.log

# Zuul PoC runtime state (bare repos + working checkout .git, both generated
# by zuul/scripts/seed-repos.sh - never source of truth, always reproducible)
zuul/gitserver-repos/
zuul/zuul-config/.git/

# Artifacts / agent runs (ephemeral job outputs)
artifacts/
/tmp/agent-*.json

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store

# Env
.env
.env.*
!.env.example

# Run API's local run_id -> newrev index (Phase 5, plan §8) - regenerated
# per Run API instance, never source of truth.
run-api-index.json

# Phase 6: `make demo` transcripts (reproducible on demand, not source of truth).
zuul/.demo-transcripts/

