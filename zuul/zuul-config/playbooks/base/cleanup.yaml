- hosts: localhost
  gather_facts: false
  tasks:
    - name: "Cleanup no-op (cleanup-run is deprecated; this is the post-run cleanup=true playbook)"
      debug:
        msg: "Cleanup for build {{ zuul.build }}"
