// Phase 5: HTTP server (Fastify routes for POST /runs, GET /runs/:id, etc.)
// implemented here. Phase 2 only delivers the git-writer library module —
// see git-writer.ts — importable by this future app.
export {};
